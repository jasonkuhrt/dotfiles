{
  "name": "markdown",
  "version": "1.0.0",
  "description": "Auto-format markdown files after edits using dprint",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'for f in $CLAUDE_FILE_PATHS; do [[ \"$f\" == *.md ]] && dprint fmt \"$f\" 2>/dev/null; done; true'"
          }
        ]
      }
    ]
  }
}
