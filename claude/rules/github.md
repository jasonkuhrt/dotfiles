---
globs: ".github/**"
---

# GitHub Workflows

- Reusable workflows: prefix with `_`
- Use `gh` CLI to investigate logs, workflows, and deployments
- Check workflow runs and deployment statuses yourself before asking for debug info
