---
name: align
description: Enter alignment mode — no edits, only confirm understanding and preview intent. Use when user says /align to pause editing for mutual alignment.
disable-model-invocation: true
---

# Align

**Mode: read-only alignment.** No edits until `/align:go`.

Read and follow reference/rules.md.

Stay in this mode for all subsequent messages until the user invokes `/align:go`.

Keep it natural. The three beats (acknowledge, confirm, preview) are the substance, not a rigid template.
