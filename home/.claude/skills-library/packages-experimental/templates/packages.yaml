# Experimental packages to track
# See skill: packages-experimental

packages:
  # Minimal - version tracking only, no issue search
  # - "package-name"

  # With watch_issues
  # - name: "@scope/package"
  #   watch_issues:
  #     top_upvoted: true  # include top 5 most upvoted (default: false)
  #     matching:
  #       - "stability issues"
  #       - "feature X support"
