# Moved

`git-agent` now lives in:

`/Users/jasonkuhrt/projects/jasonkuhrt/git-agent`

That repo is the source of truth for:

- the CLI implementation
- the operator manual (`git-agent book`)
- the specification
- the end-to-end regression suite

Dotfiles is now only a consumer.
