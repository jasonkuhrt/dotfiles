#!/usr/bin/env bash
set -euo pipefail

repo_root="/Users/jasonkuhrt/projects/jasonkuhrt/git-agent"
target="$repo_root/git-agent"

if [ ! -x "$target" ]; then
  printf 'fatal: git-agent source checkout is missing: %s\n' "$target" >&2
  exit 1
fi

exec "$target" "$@"
