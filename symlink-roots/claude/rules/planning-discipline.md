# Planning Discipline

Always do the complete cut — never "move now, remodel later." PR size is not a concern. Backwards compatibility is forbidden unless explicitly required. Only constrain scope if the user says so.
